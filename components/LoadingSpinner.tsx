const LoadingSpinner = () => (
  <div
    aria-label="Loading"
    className="size-8 animate-spin rounded-full border-4 border-blue-100 border-t-bankGradient"
    role="status"
  />
);

export default LoadingSpinner;
